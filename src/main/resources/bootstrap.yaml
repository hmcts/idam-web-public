spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/idam-idam
      aliases:
        web-admin-client-secret: SECURITY_OAUTH2_CLIENT_CLIENTSECRET
